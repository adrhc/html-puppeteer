class RowValues {
    values;
    index;

    /**
     * @param {*} values
     * @param {number} index
     */
    constructor(values, index = 0) {
        this.values = values;
        this.index = index;
    }
}