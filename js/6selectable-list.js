$(() => {
    // dogs table with both read-only and editable row
    JQWUtil.createComponents();
    /*new SelectableListComponent({
        elemIdOrJQuery: "dogsTable", items: DbMocks.DOGS
    });*/
});