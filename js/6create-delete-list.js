$(() => {
    JQWUtil.createComponents();

    // console.log(JSON.stringify(DbMocks.dogsOf(5)));
    // console.log(JSON.stringify(DbMocks.personsOf(5)));

    // new CreateDeleteListComponent({elemIdOrJQuery: "dogsTable"});
});
