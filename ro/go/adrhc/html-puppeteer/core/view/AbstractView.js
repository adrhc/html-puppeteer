export default class AbstractView {
    /**
     * @param {{}} values
     */
    create(values) {}

    /**
     * @param {{}} values
     */
    replace(values) {}

    /**
     * handles REMOVED state change event
     */
    remove() {}
}