export default class AbstractView {
    /**
     * @param {{}} values
     */
    create(values) {}

    /**
     * @param {{}} values
     */
    replace(values) {}

    /**
     * removes the DOM element
     */
    remove() {}
}