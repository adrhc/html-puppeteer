export default class StateChangesHandler {
    /**
     * @param {StateChange} stateChange
     */
    created(stateChange) {}

    /**
     * @param {StateChange} stateChange
     */
    modified(stateChange) {}

    /**
     * @param {StateChange} stateChange
     */
    removed(stateChange) {}
}